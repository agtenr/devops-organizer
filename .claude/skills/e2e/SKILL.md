<!-- AIND KICKSTART DRAFT — intended design captured in conversation, NOT yet validated against
     code. Review and correct before relying on it; re-run /aind:onboard once code exists to
     reconcile. Suggestions, not ground truth. -->
---
name: e2e
description: Run the Playwright end-to-end UI tests. Aspirational — Playwright is not set up yet.
allowed-tools: Bash
---

# End-to-end UI tests (Playwright)

> **STUB — ASPIRATIONAL, TODO: not set up yet.** Playwright is an intended addition (see
> `.claude/rules/testing.md`); neither the dependency nor this script exists. Do not assume
> it runs.

```bash
npm run test:e2e   # expected (once added): Playwright test run
```
