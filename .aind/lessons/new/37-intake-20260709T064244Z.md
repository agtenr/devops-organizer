---
id: 37-intake-20260709T064244Z
work_item: 37
agent: intake
phase: intake
severity: observation
source: self-report
area: .claude/intake-rubric.md
---

## Observation
On story #37 the two dependency signals diverged in a way that reads as contradictory to an author: the automated dependency gate reported DEPS_VERDICT: NONE (clear, because it only reads structured Predecessor links), yet objective criterion O5 declined the same story for the same dependency (which was expressed in prose plus a generic Related link, and #36 is already implemented). So a fully well-defined story whose only real blocker is already done still gets declined on a link-type technicality, and the two mechanisms — the deps gate and O5 — give opposite readings of the identical dependency. Authors in ADO reach for "Related" links by default and do not naturally distinguish Predecessor from Related, so this gap is likely to recur; the friction is not that O5 is ambiguous (its wording is explicit) but that its notion of a "named dependency" is narrower than both ordinary ADO usage and the deps gate's own NONE/clear result.
