---
id: 125-coder-20260925T105009Z
work_item: 125
agent: coder
phase: build
severity: observation
source: self-report
area: .claude/rules/testing.md
---

## Observation
A story whose acceptance criterion lives entirely on the pre-sign-in / auth-loading screen (AB#125: dark theme flash before the real Entra ID sign-in completes) cannot be live-verified by the coder at all, headlessly or otherwise, because the app's real auth gate needs a live Entra ID account. It also cannot be captured by the existing mock-auth harness seam (`src/harness.tsx`), which testing.md documents as bypassing MSAL and ThemeProvider entirely — so it structurally cannot render the sign-in loading screen or a themed pre-auth state. The plan correctly flagged this DoD item as "cleared by hand," but the coder had no way to attempt even a partial automated check (a Playwright probe against the real dev server just captures the pre-JS blank page, since MSAL redirects within ~300ms). This is a recurring gap for any future story whose acceptance criterion concerns the pre-auth/loading UI specifically.
