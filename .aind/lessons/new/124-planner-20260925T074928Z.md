---
id: 124-planner-20260925T074928Z
work_item: 124
agent: planner
phase: plan
severity: observation
source: self-report
---

## Observation
On resuming AB#124 in the local flow, the status tag already read "Plan ready for review" (set by an earlier, interrupted run), but no story branch and no plans/124/plan.md existed anywhere in git history or on disk. The status transition and the branch/plan commit are two separate steps in the local-flow script sequence, so an earlier run that got interrupted between them left the work item pointing at a phase whose artifact never landed. I had to detect this by cross-checking `aind-open-code-pr.sh resume-local` (which correctly reported "no local story branch") against the work item's stale status, then proceed as a fresh create. A planner re-run in local flow can't fully trust the status tag alone to mean "the branch/plan exists" — it should always confirm via the branch-existence check before deciding create vs. revise.
