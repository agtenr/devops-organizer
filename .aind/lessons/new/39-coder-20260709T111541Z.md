---
id: 39-coder-20260709T111541Z
work_item: 39
agent: coder
phase: build
severity: correction
source: https://github.com/agtenr/devops-organizer/pull/19#issuecomment-4924432459
area: .claude/rules/frontend-architecture.md
---

## Observation
The reviewer flagged (non-blocking) that useSidebarFilters.ts used the `use` prefix but exported only pure functions with no React hook. The human acted on it and directed the structural resolution, explicitly deferring the "how" to the coder: rename off the `use` prefix, keep the file colocated in the component's own folder (not a new src/utils/ folder, since it is feature-specific facet logic, and not src/services/ which is reserved for the categorization business logic), and keep it a pure functional module rather than introducing a utility class. Chosen name: facetFilters.ts — also because sidebarFilters.ts collided case-insensitively with the SidebarFilters.tsx component in the same folder (TS1149). Signal: frontend-architecture.md says "logic lives in a colocated custom hook (e.g. useCustomerTabs.ts)" and the project has repeatedly produced `use*`-named files that hold pure, hook-free logic; the human's ruling is that such files should drop the `use` prefix and be named for their content while staying colocated as functional modules.
