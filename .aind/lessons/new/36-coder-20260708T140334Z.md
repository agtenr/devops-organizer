---
id: 36-coder-20260708T140334Z
work_item: 36
agent: coder
phase: build
severity: observation
source: self-report
area: .claude/rules/testing.md
---

## Observation
The only unit test on main, src/App/App.test.tsx, is stale and red: it still asserts a "hello, devops organizer" heading that story 30 removed, and renders <App/> without an MsalProvider so it also throws on context. Story 30's plan had an explicit task to update this test when the hello-world screen was replaced, but the merged code left it unchanged, so `npm run test` has been failing on the integration branch independent of any later story. Cause: nothing in the flow gates the test suite between stories (build and lint are run, tests are deferred/ungated in the implement iteration), so a superseded test rots silently and each subsequent story inherits a red suite it did not cause.
