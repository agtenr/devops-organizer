---
id: 57-coder-20260715T141908Z
work_item: 57
agent: coder
phase: build
severity: observation
source: self-report
area: .claude/rules/testing.md
---

## Observation
A test was silently failing on the integration branch (main): Organizer.test.tsx asserted getByRole('table', {name:'E-mails'}) but story 54 migrated EmailList to a Fluent DataGrid (role=grid), so the success-case assertion had been red on main since that merge. Because the project has no CI gate keeping main's Vitest suite green, the stale assertion went undetected until an unrelated story (57) whose DoD requires a green full suite had to fix it. The recurring signal: role-based test assertions drift silently when a component's underlying primitive changes and nothing re-runs the full suite on the integration branch.
