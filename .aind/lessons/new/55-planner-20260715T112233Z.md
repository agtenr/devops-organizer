---
id: 55-planner-20260715T112233Z
work_item: 55
agent: planner
phase: plan
severity: observation
source: self-report
area: .claude/rules/frontend-architecture.md
---

## Observation
Story 55's acceptance criterion literally asked the preview to close "when switching filters", which directly reverses a decision another story (40) ratified and locked in code: useEmailList deliberately keeps the preview showing after a filter change removes the row (documented at useEmailList.ts:63 and pinned by useEmailList.test.ts:67). A new story's AC can silently contradict a prior ratified behaviour recorded only in a hook doc-comment and a test — nothing in the story text flags the conflict, so the planner only catches it by reading the target code. The signal: ratified cross-story UX decisions live as prose in code comments/tests, not in a durable rules file, so they are easy for a later story to unknowingly overturn.
