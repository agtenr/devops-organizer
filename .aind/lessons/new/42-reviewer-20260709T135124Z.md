---
id: 42-reviewer-20260709T135124Z
work_item: 42
agent: reviewer
phase: build
severity: observation
source: https://github.com/agtenr/devops-organizer/pull/24
area: .claude/rules/authentication.md
---

## Observation
The code added a new Graph scope (Files.ReadWrite) with the rationale in code comments, but authentication.md's Scope staging section — explicitly the living record of granted scopes (stories 30, 36) — was not updated in the same change; the reviewer had to flag it. This recurs because scope/permission changes land in code (msalConfig/graphClient) while the rule file that documents the scope-staging history is a separate artifact that is easy to leave stale, so the granted-scope record and the code drift apart unless the coder remembers to update both.
