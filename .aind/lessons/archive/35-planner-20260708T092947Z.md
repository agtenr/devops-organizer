---
id: 35-planner-20260708T092947Z
work_item: 35
agent: planner
phase: plan
severity: observation
source: self-report
area: .claude/rules/frontend-architecture.md
---

## Observation
The frontend-architecture "UI layout invariants" rule states the top bar shows "only the logged-in user's display name and a log-out button — nothing else." Story 35 — the very next top-bar story — explicitly requires a centered app title, directly contradicting that absolute wording (which story 30 had also copied verbatim into TopBar.tsx's doc-comment). The invariant was written more restrictively than the product actually intends, so a later story had to reconcile it. Cause: an early rule froze a partial UI as an absolute ("nothing else") before the full top-bar design was specified.
