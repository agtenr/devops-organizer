---
id: 38-planner-20260709T094606Z
work_item: 38
agent: planner
phase: plan
severity: correction
source: https://github.com/agtenr/devops-organizer/pull/16#discussion_r3550565246
area: .claude/rules/frontend-architecture.md
---

## Observation
The planner colocated the fetch+categorize data path inside the container's useOrganizer hook, reasoning that only the container consumes it today and extraction would be speculative. The human overruled with a general principle: "If you know data needs to be re-used, always extract it from the component level" — since #39's real list view is a known future consumer, the data hook should be extracted to src/hooks/ up front rather than colocated and moved later. Cause: the planner optimized for minimal-surface-now over a known, documented future reuse, where the project convention is to hoist shared data hooks as soon as reuse is foreseeable.
