---
id: 124-planner-20260925T064432Z
work_item: 124
agent: planner
phase: plan
severity: observation
source: self-report
---

## Observation
Story 124 attached a color-palette image as the concrete spec for a styling change. The image is an ADO work-item attachment behind auth, so a generic web-fetch tool cannot read it; I had to manually curl it with the project PAT (basic auth, empty username) into a temp file, then sample exact pixel hex values with a small PowerShell System.Drawing script (no Python available in this environment). This worked, but it's a non-obvious multi-step workaround a planner has to improvise each time a story's key spec lives in an attached image rather than text.
