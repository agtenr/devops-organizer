---
id: 35-coder-20260708T094919Z
work_item: 35
agent: coder
phase: build
severity: observation
source: self-report
area: .claude/rules/testing.md
---

## Observation
The merged plan's Task breakdown (tasks 3 & 4) and Definition of done included behavioral-test authoring and stale-test repair, but the /aind:implement command mandates "this iteration writes no tests." So the code PR necessarily shipped with those test tasks and their DoD items deferred, and the pre-existing red baseline suite left red. Cause: the plan phase folds test work into the story's tasks/DoD while the implement phase defers all test authoring to a later step, so every implement run on a plan like this reports intentionally-unmet DoD items and must explain the divergence in the PR.
