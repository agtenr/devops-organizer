---
id: 40-coder-20260709T125405Z
work_item: 40
agent: coder
phase: build
severity: correction
source: https://github.com/agtenr/devops-organizer/pull/21
area: .claude/rules/frontend-architecture.md
---

## Observation
The plan's Data-contracts section placed the pure display helpers (formatReceivedDate/resolveBody/ResolvedBody) inside the colocated hook file useEmailList.ts. The human directed extracting them into their own file (emailFormatters.ts), explicitly citing the existing SidebarFilters.tsx / facetFilters.ts split as the pattern to follow. The signal: this codebase's convention is that pure, React-free, unit-testable helpers live in their own colocated file separate from the custom hook — not bundled into the hook — yet the plan (and the initial implementation) co-located them in the hook. Planners keep specifying "helpers in useXxx.ts" when the established codebase pattern is a separate pure-util module.
