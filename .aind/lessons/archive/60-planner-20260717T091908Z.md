---
id: 60-planner-20260717T091908Z
work_item: 60
agent: planner
phase: plan
severity: observation
source: self-report
area: .claude/rules/testing.md
---

## Observation
The screenshot/E2E harness seam (src/harness.tsx) mounts the real Organizer shell via the useData injection, but replaces the top bar with a STATIC stand-in header because the real TopBar depends on MSAL. testing.md frames the harness as "renders the real gated shell" and mandates screenshots/E2E through it, but any story whose acceptance lives on the TopBar itself (this story: title text + click-to-refresh) cannot be faithfully screenshot- or E2E-verified through the harness — the shot shows a stand-in, not the shipped component, and interactive TopBar behavior has no harness vehicle at all. This forced falling back to a unit test + manual live-verification for the click behavior. The cause is that the harness seam's coverage deliberately excludes the MSAL-backed top bar, a limitation the testing rule does not call out, so planners must rediscover it per top-bar story.
