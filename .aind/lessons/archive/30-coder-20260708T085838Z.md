---
id: 30-coder-20260708T085838Z
work_item: 30
agent: coder
phase: build
severity: observation
source: self-report
area: .claude/rules/frontend-architecture.md
---

## Observation
The scaffold (story 29) shipped without a src/vite-env.d.ts, so import.meta.env is untyped. The first story to read a VITE_* env var (this auth story) could not type-check under `tsc -b` until that file was added with `/// <reference types="vite/client" />` plus an ImportMetaEnv interface. Cause: the scaffold created the Vite React-TS project files but omitted the standard vite-env.d.ts that `npm create vite` normally generates, so env typing is silently missing until a consumer appears — the plan assumed import.meta.env access would just work.
