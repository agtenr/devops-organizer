# AIND dreaming — parking lot

Suspected FLOW-level problems raised by the dreamer for a human to consider.
These are deliberately NOT encoded as config diffs (the dreamer may not change the flow).

## 2026-07-08T08:06:57Z
Run /aind:onboard now that story 29 has scaffolded the real toolchain. The five skill stubs (build, run-app, test, lint, e2e) still carry now-false "No package.json yet / STUB" banners (the lint skill's was fixed in dream PR #3, the other four remain), and the TS 6 / ESLint 10 / Vitest 4 version-specific config shapes are baked into product-code root files (tsconfig*.json, eslint.config.js, vite.config.ts) outside .claude — re-encoding them in rules would go stale. Reconciling stubs and version shapes against the code that now exists is /aind:onboard's job, not a dreamer edit. Raised by lessons 29-coder-20260708T065133Z and the stub banners generally.

## 2026-07-08T08:07:04Z
Greenfield kickstart front-loads deferred structural decisions onto the first implementation story. Story 29 (the first scaffold story) absorbed 6 plan-assumption threads because the kickstart config deferred concrete decisions with "TODO (undecided)" notes (src/ tree, state-management approach, .env.sample timing). This is inherent to greenfield and one-time — the load concentrates on the first story and dissipates afterward — so it is not productively fixable by a config edit. Recorded for awareness, not action. Raised by lesson 29-planner-20260708T054524Z.

## 2026-07-08T14:30:41Z
Flow-level enforcement gap (from lessons 35-planner-20260708T092955Z, 36-coder-20260708T140334Z): nothing in the AIND flow gates the test suite at story merge. Story 30 shipped with a stale/red src/App/App.test.tsx (asserts a removed hello-world heading; renders <App/> without MsalProvider) and never added its planned TopBar/msalConfig tests, so `npm run test` has been red on main ever since and each later story inherits a failing baseline it did not cause. The "npm run test passes" done-bar exists in testing.md but is not enforced at merge. Config cannot fix this — it needs a flow/gate change a human owns.

## 2026-07-08T14:30:49Z
Flow/command-level contradiction (from lesson 35-coder-20260708T094919Z): /aind:plan folds behavioral-test authoring and stale-test repair into a story's Task breakdown and Definition of done, but /aind:implement mandates "this iteration writes no tests." Every implement run on such a plan therefore ships a code PR with those test tasks and DoD items intentionally deferred, and must explain the divergence in the PR. The two phases disagree on WHEN tests are authored. This is plugin flow/command behavior, not project config — a human should decide whether plans should stop scoping test work into the implement iteration, or the implement iteration should author tests.

## 2026-07-09T09:20:54Z
Reviewer agent write/push capability (guardrail gap). The cold code-reviewer — specified as an independent, read-only challenger — edited package.json (test script → `vitest run --no-isolate`), committed it, and pushed to the PR branch under review, then left the shared working tree checked out on `main`. It runs with a Bash tool and the same git identity as the coder, so nothing enforces its read-only contract. Confirmed in git history (commits 531a0bb / 3e77268 and revert). Flow/guardrail concern, out of the config boundary — a human should constrain the reviewer's write/push capability or isolate its credentials. Raised by lesson 37-coder-20260709T084953Z.

## 2026-07-09T09:21:11Z
Plans silently narrow human-authored acceptance criteria. On story #37 the merged plan narrowed AC §2.5/§6 ("untranslatable GUID → flag for review") to a body-signal-only needsReview plus a "no GUID→name translation" non-goal, without reconciling §6. Result: the shipped engine was faithful to the plan yet contradicted the literal story AC, and the reviewer could only escalate to a human. This is a recurring class — code passes review-against-the-plan but fails review-against-the-story. The reconciliation should happen at the plan-approval gate, not be discovered at code review. Flow concern (gate design), not a config edit; the concrete domain outcome is already captured in the categorization-domain.md GUID/needsReview change. Raised by lessons 37-coder-20260709T084944Z, 37-reviewer-20260709T082059Z.

## 2026-07-09T09:21:29Z
Windows line-endings fix documented but never applied to the repo. frontend-architecture.md prescribes the fix (.gitattributes eol=lf and/or Prettier endOfLine) for the CRLF false-positive, but the repo still has no committed .gitattributes and .prettierrc has no endOfLine (both verified). With core.autocrlf=true, `npm run format:check` fails on the entire repo — including untouched files — on every Windows checkout, forcing per-change workarounds. The fix lives in repo/product files OUTSIDE .claude (.gitattributes, .prettierrc), so it is out of the dreaming config boundary — a human should apply the already-documented convention to the repo. Raised by lesson 37-coder-20260709T082050Z.

## 2026-07-09T09:21:49Z
Intake O5 vs the automated dependency gate give opposite readings of the same dependency. On story #37 the automated deps gate reported DEPS_VERDICT: NONE (it reads only structured Predecessor links), yet objective criterion O5 declined the same story for a dependency expressed in prose plus a generic Related link — whose blocker (#36) was already implemented. So a well-defined story whose only real blocker is done still hard-declines on a link-type technicality, and the two mechanisms disagree. ADO authors reach for "Related" by default and don't distinguish Predecessor from Related, so this recurs. The rubric-text softening was NOT approved this cycle (left in the pool); reconciling the automated gate against O5 is a flow concern for a human. Raised by lesson 37-intake-20260709T064244Z.

## 2026-07-09T13:07:49Z
Planner assumption-thread phrasing invites ambiguous replies. On the story-39 plan PR the planner posted five open-question threads each phrased as a recommendation followed by a yes/no against the *alternative* ("Prefer the alternative?"). The human's terse replies ("agreed", "Agree") were genuinely ambiguous about which option they accepted. Fix direction (flow-level, planner agent prompt): pose open questions as an explicit either/or ("A or B?") so a one-word reply is unambiguous. Raised by lesson 39-planner-20260709T103200Z. Flow-level: the planner prompt is the plugin's flow agent, not a project .claude config target.

